# 대시보드(app.py) 실행에 필요한 최소 패키지
# 분석 스크립트(src/)를 돌리려면 requirements-dev.txt 를 함께 설치할 것
#
# 버전을 고정한 이유
#   배포 환경이 매번 최신 버전을 설치하면서 streamlit 내부가 호출하는 starlette의
#   시그니처가 바뀌어 앱이 기동에 실패한 적이 있음
#   (TypeError: GZipResponder.__init__() missing 'thread_minimum_size').
#   로컬에서 동작을 확인한 조합으로 고정해 재현 가능하게 함.
streamlit==1.45.1
pandas==2.2.3
numpy==2.1.3
plotly==5.24.1
